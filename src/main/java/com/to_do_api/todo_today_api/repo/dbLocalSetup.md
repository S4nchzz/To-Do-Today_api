```SQL
CREATE DATABASE TO_DO_TODAY;
USE TO_DO_TODAY;

CREATE TABLE USER (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    USERNAME VARCHAR(20) NOT NULL,
    SALT VARCHAR(16) NOT NULL,
    PASS BINARY(32) NOT NULL,
    EMAIL VARCHAR(50) NOT NULL,
    EMAIL_VERIFIED BOOLEAN NOT NULL,
    LOGGED_IN BOOLEAN NOT NULL,
    INGROUP BOOLEAN NOT NULL,
    CONSTRAINT uniqueName UNIQUE (username),
    CONSTRAINT uniqueEmail UNIQUE (email)
);

CREATE TABLE USER_PROFILE (
    ID INT PRIMARY KEY,
    PROFILE_PICTURE MEDIUMBLOB,
    PHONE VARCHAR(50),
    FOREIGN KEY (ID) REFERENCES USER(ID)
);

CREATE TABLE USER_TEMPORAL_TOKEN (
    TOKEN VARCHAR(255) NOT NULL,
    USERID INT,
    FOREIGN KEY (USERID) REFERENCES USER(ID)
);

CREATE TABLE KEEP_LOGGED_TOKENS(
    TOKEN VARCHAR(255) NOT NULL,
    USERID INT,
    FOREIGN KEY (USERID) REFERENCES USER(ID)
);

CREATE TABLE AUTH_API_TOKENS (
    ID INTEGER PRIMARY KEY AUTO_INCREMENT,
    TOKEN VARCHAR(255) NOT NULL
);

CREATE TABLE TEAMS(
    TEAMKEY VARCHAR(4) PRIMARY KEY,
    NAME VARCHAR(30),
    DESCRIPTION VARCHAR(255),
	ADMINISTRATOR INTEGER NOT NULL,
    PUBLICGROUP BOOLEAN NOT NULL,
    PASSWORD VARCHAR(255),
    DATE VARCHAR(255) NOT NULL,
    FOREIGN KEY (ADMINISTRATOR) REFERENCES USER(ID),
    CONSTRAINT uniqueName UNIQUE (name)
);

CREATE TABLE CLIENT_TEAM_ASSOCIATION(
    ID INTEGER PRIMARY KEY AUTO_INCREMENT,
    USERID INTEGER NOT NULL,
    TEAMKEY VARCHAR(4) NOT NULL,
    FOREIGN KEY (USERID) REFERENCES USER(ID),
    FOREIGN KEY (TEAMKEY) REFERENCES TEAMS(TEAMKEY)
);

CREATE TABLE TODO (
    ID INTEGER PRIMARY KEY AUTO_INCREMENT,
    USERID INT NOT NULL,
    HEADER VARCHAR(255) NOT NULL,
    CONTENT VARCHAR(255) NOT NULL,
    DATE VARCHAR(255) NOT NULL,
    FAV BOOLEAN,
    ENDED BOOLEAN NOT NULL,
    FOREIGN KEY (USERID) REFERENCES USER(ID)
);

CREATE TABLE TEAM_TODO(
    ID INTEGER PRIMARY KEY AUTO_INCREMENT,
    TEAMKEY VARCHAR(4) NOT NULL,
    HEADER VARCHAR(255) NOT NULL,
    CONTENT VARCHAR(255) NOT NULL,
    DATE VARCHAR(255) NOT NULL,
    FAV BOOLEAN,
    ENDED BOOLEAN NOT NULL,
    FOREIGN KEY (TEAMKEY) REFERENCES TEAMS(TEAMKEY)
);

CREATE TABLE VERIFY_EMAIL_CODES (
    ID INTEGER PRIMARY KEY AUTO_INCREMENT,
    CODE INTEGER NOT NULL,
    EMAIL VARCHAR(50) NOT NULL,
    FOREIGN KEY (EMAIL) REFERENCES USER(EMAIL)
);
```